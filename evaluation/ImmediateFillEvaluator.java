package evaluation;

import core.*;

public class ImmediateFillEvaluator implements Evaluator {

    @Override
    public double eval(Game g) {
        int numFilled = 0;
        return numFilled;
    }
}
